import React from "react";

let card = () => {
  return (
    <div>
        ` ${product.name}
        ${product.price}
        `
     
    </div>
  )
};

const Products = () => {
    return (
        <div>

        </div>
    )
};

//join email list popup